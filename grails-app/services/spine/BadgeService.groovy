package spine

class BadgeService {

    static transactional = false

    /**
     * based upon tag list, badges will be returned; at the moment we just hardcore some value
     * the badges are basically strings, which need a corresponding picture with an added .jpg
     * 
     * @return
     */
	def evaluateTags(HashMap taglist) {

    }
}
